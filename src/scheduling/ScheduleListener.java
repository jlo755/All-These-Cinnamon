package scheduling;

/**
 * Interface for a listener that updates upon being notified of a change in the optimal scheduling calculations.
 *
 */
public interface ScheduleListener {
	public void update();
}
